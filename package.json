{
    "name": "servicetagger",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "postinstall": "cd server && npm ci --prefer-offline && npx prisma generate",
        "vercel-build": "cd client && npm run build"
    },
    "devDependencies": {
        "@vercel/node": "^5.5.16"
    }
}